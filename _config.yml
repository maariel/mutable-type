theme: jekyll-theme-minimal

title: Mutable Type 
description: A repository of poetry in progress.

defaults:
  -
    scope:
      path: "" # all files
      type: "posts"
    values:
      layout: "default"
